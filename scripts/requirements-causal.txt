# Causality tier — DoWhy + EconML + graphviz Python wrapper.
# Requires the Graphviz *system* binary for DAG rendering:
#   brew install graphviz   (macOS)
#   apt install graphviz    (Linux)
#   winget install graphviz (Windows)

dowhy>=0.11
econml>=0.15
networkx>=3.0
graphviz>=0.20
pydot>=2.0
scikit-learn>=1.3
scipy>=1.11
